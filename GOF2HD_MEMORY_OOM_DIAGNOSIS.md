# GOF2HD — диагностика зависаний и OOM (2026-08-16)

Технический отчёт по проблеме: **игра периодически зависает/умирает через ~10-17 мин
активной игры** (симптом пользователя: «~4 мин — весь девайс не реагирует + одно ядро
на 100%», «~10 мин — игра виснет намертво»). Пользователь связывал появление с фиксом
звука, но по факту звук не причина (см. ниже).

## Вывод (коротко)

**Игра убивается ядром по OOM** (Out Of Memory). Источник — **GPU-память драйвера mali
(маппинги `/dev/mali0`)**: драйвер не возвращает маппинги удалённых текстур/буферов
(VA растёт), а игра рисует ими непрерывно, из-за чего страницы физически закрепляются
(до ~650 МБ VA / ~800 МБ file-rss). Устройство — 973 МБ RAM без swap/zram, ядро не
может рециклировать GPU-память (`pages_scanned:0`, unevictable) → OOM/зависание через
10-40 мин активной игры.

Игра **стабильна в обычной игре**: сессии 16-23+ минут с VmRSS ~130-200 МБ и mali0
~60-140 МБ. OOM происходит при длительной игре с перелётами между планетами
(стабильный триггер) — накопление GPU-маппингов опережает освобождение.

## Устройство

- ANBERNIC, armhf, ядро aarch64 4.9.170, RAM 973 МБ (DMA-зона ~1 ГБ), без swap/zram.
- Проект: `/root/gof2hd/`, собранный стек в `port/run-native/`.
- GPU: `/usr/lib32/libmali.so` (kbase), debugfs `/sys/kernel/debug/mali0/`.

## Пойманные OOM (dmesg, два независимых прогона)

```
[ pid ] uid tgid total_vm rss ... name
[20528] gof2hd total-vm:786928kB anon-rss:41488kB file-rss:806624kB
Out of memory: Kill process 20528 (gof2hd) score 826
oom_reaper: reaped 20528, now anon-rss:0kB, file-rss:829028kB

[2984] gof2hd total-vm:835348kB anon-rss:49804kB file-rss:813880kB
Out of memory: Kill process 2984 (gof2hd) score 842
```

- **anon-rss всего ~40-50 МБ** — куча процесса мала.
- **file-rss ~810-830 МБ** — file-backed страницы процесса.
- Триггер OOM-киллера — аллокация в `mali_kbase`:
  `kbase_native_mgm_alloc → kbase_mem_alloc_page → kbase_mem_pool_alloc_pages`.

## Ключевые факты диагностики

### 1. Mem-Info на момент OOM

```
Node 0 active_anon:82240kB inactive_anon:1328kB active_file:1404kB inactive_file:1456kB
DMA free:47856kB ... present:1048576kB managed:996628kB
pages_scanned:0 all_unreclaimable? no
```

- Глобальный **page cache почти пуст** (active_file ~1.4 МБ) — т.е. file-rss процесса —
  это НЕ обычный file-кэш чтений.
- **~828 МБ DMA-зоны «неучтено»** (не free/anon/file/slab) — это и есть «file-rss»
  процесса, и это **GPU/device-память (маппинги /dev/mali0 + CMA)**, которую ядро не
  рециклирует.
- `pages_scanned:0` — ядро не сканировало ни одной страницы перед OOM: память
  считалась нерециклируемой (unevictable), поэтому `kill` вместо reclaim.

### 2. Рост GPU-памяти (mali0) — взрыв за ~11 секунд

Сэмплер (по `/proc/PID/maps`, сумма маппингов `/dev/mali0`):

```
t=...572  VmRSS:=171488 mali0MB=101   (текстуры: liveTex=51, tex2d=290, comp=2369 — стабильно)
t=...583  VmRSS:=553500 mali0MB=456   (текстуры: liveTex=51, tex2d=290, comp=2369 — те же)
```

- **GPU-память выросла на ~355 МБ за ~11 секунд**, при этом счётчики GL-объектов
  (генерация текстур/буферов) на последнем логированном отсчёте заморожены.
- Никаких одиночных «гигантских» аллокаций: `BIG compressed (>4 МБ)`, `BIG tex2d
  (>4096²)`, `BIG bufferData (>16 МБ)` не было. Значит — **пачка средних текстур**
  (загрузка сектора), суммарно ~350 МБ, уложенная в один-два кадра/десяти секунд.

### 3. Текстуры и буферы НЕ текут

Инструментация в `gles-stub/gles-bridge.c` (счётчики `[gltex]`):

- **Текстуры**: gen/del сбалансированы, liveTex ~30-70, держится стабильно.
- **FBO/renderbuffer**: по 1, без роста.
- **VBO**: `glGenBuffers`/`glDeleteBuffers` сбалансированы (vboGen растёт вместе с del);
  `glBufferData` вызывается постоянно (~30-50/с) — это пересоздание одних и тех же
  динамических буферов, суммарно ~80 МБ, не растёт.
- GPU (debugfs `gpu_memory` + маппинги) в стабильных сессиях колеблется 60→130→84→100 МБ,
  т.е. движок **освобождает** текстуры при уходе из зоны. Утечки нет.

### 4. Чем OOM-прогоны отличаются от стабильных

- OOM-прогон: 51 живая текстура × ~9 МБ ≈ **460 МБ GPU**.
- Стабильный прогон: 53 живые текстуры × ~2 МБ ≈ **~100 МБ GPU**.

То есть дело в **размере текстур конкретной локации**, а не в их количестве/утечке.
Все 3D-текстуры в OBB уже лежат в `assets/data/assets/main/3d/textures/low/`
(единственный вариант, ~4.2 МБ максимум на файл ETC).

### 6. Дефинитивный дамп в момент зависания (watchdog, /proc/self/maps)

При перелёте между планетами (игра с `oom_score_adj=-1000`, чтобы не была убита и
успела зависнуть; watchdog снял состояние):

```
VmPeak: 936980 kB   VmRSS: 901752 kB
RssAnon:  4904 kB   RssFile: 896848 kB   VmSwap: 50140 kB

/dev/mali0: 10950 маппингов, 738 MB
  <64KB: 8142    64-256KB: 2387    256KB-1MB: 421    >1MB: 0
```

- **RssAnon всего ~5 МБ** — куча процесса мала.
- **RssFile ~897 МБ** — почти вся память это GPU/device-страницы.
- **10 950 маппингов /dev/mali0, все <1 МБ (8142 < 64 КБ)** — тысячи КРОШЕЧНЫХ
  GPU-аллокаций (VBO/буферы объектов сектора), а не большие текстуры.
- Ни одного маппинга >1 МБ — «больших текстур» как таковых нет; это дроблёная
  мелкая аллокация (аномально много отдельных буферов).

### 7. Что НЕ помогает (проверено)

- **swap-файл 384 МБ** — не помогает: GPU/device-страницы не своппятся (VmSwap ~0-50 МБ).
- **`oom_score_adj=-1000`** — не спасает: ядро не убивает игру, но она ВИСНЕТ
  (аллокация не может выделить страницы) → девайс замирает надолго. Хуже смерти.
- **`drop_caches` каждые 15 с** — не помогает: взрыв происходит в GPU-памяти,
  а не в file-кэше (file-кэш уже мал на момент OOM).
- **`GOF_DISPLAY=WxH` (меньшее разрешение)** — не меняет выбор текстур
  (движок всё равно грузит `gof2_interface.aei` и `textures/low/*`).
- **Флага качества через JNI нет**: движок спрашивает только `DeviceInfo.isPad`,
  `getDisplayWidth/Height` (проверено `GOF_VERBOSE_JNI=1`).
- 3D-текстуры уже единственный вариант `textures/low/` (500 файлов).
- CMA всего 64 МБ, лимита GPU-памяти в драйвере mali нет (только пулы kbase,
  не влияют).

### 8. Вывод о причине

**Игре для перелёта между планетами требуется ~700-900 МБ GPU-памяти**
(тысячи мелких GPU-буферов объектов). На устройстве доступно ~770 МБ
(973 - ~150-200 МБ системы). Это **ресурсный потолок девайса**, а не утечка
в нашем коде: текстуры уже low, качество не переключается, GPU-память
нерециклируема ядром. На Android такие пики переживались за счёт zRAM/LMK и
более агрессивного reclaim; в Linux-порту ядро 4.9 при нехватке памяти убивает
процесс (OOM) или вешает его.

### 8.1. ОКОНЧАТЕЛЬНЫЙ диагноз: драйвер mali удерживает удалённые GPU-ресурсы

Точный замер при OOM (PID 2903, watchdog `/proc/self/maps` + `[gltex]`):

```
mali0: 8999 маппингов, 658 MB   (<64K:6517, 64-256K:2106, 256K-1M:370, >1M:6)
VmRSS: 863 MB   RssAnon: 43 MB   RssFile: 819 MB

живые GL-ресурсы в тот же момент:
  liveTexBytesMB=60   liveVBO=983 (vboBytesMB=16)   cubeMB=0
  накоплено за сессию: twodMB=426 (текстуры) + ~bufMB=68 (буферы) = ~494 MB
```

- **Живые GL-объекты крошечные (~76 МБ)** — движок создаёт/удаляет текстуры и
  буферы корректно (del-счётчики растут, live стабильно 30-70 текстур, ~500 VBO).
- **mali0 на ~580 МБ больше живых ресурсов** — это GPU-память **удалённых** текстур
  и буферов, которую драйвер mali (kbase) не возвращает системе за сессию.
- Куб-текстур движок не использует (cubeMB=0), пулы kbase пусты (не удержание в пуле),
  `pages_scanned:0` — ядро не может рециклировать эту память.

**Ответ на вопрос «это какой-то конкретный метод OpenGL?» — нет.** Ни один метод
(glTexImage2D/glCompressedTexImage2D/glBufferData + glDelete*) не течёт: живые
ресурсы малы. Память накапливается **в драйвере mali** (не возвращает freed-страницы),
поэтому после перезагрузки (свежий процесс) сектор грузится нормально, а через
10-40 мин игры накопление упирается в 973 МБ → OOM/зависание.

**Практичное решение:** авто-перезапуск (`port/tools/play-loop.sh`) — рестарт
сбрасывает накопление (как перезагрузка). Игра играбельна сессиями.

### 8.2. Наша сторона тоже была замешана (найдено и исправлено)

Оверлей курсора (`port/host/wrap_overlay.c`) в `overlay_draw()` каждый кадр вызывал
`glBufferData(GL_ARRAY_BUFFER, sizeof(verts), verts, GL_DYNAMIC_DRAW)` — пересоздание
VBO курсора ~30-50 раз/сек. На этом mali-драйвере каждая такая re-spec добавляет
GPU-аллокацию. Замер после фикса: на старте `liveBuf` упал с ~1,100 до ~35,
`vboGen` с ~642 до ~36. **Исправлено** — оверлей теперь рисует client-side вершинами
(без VBO и без per-frame аллокаций).

Однако главный аккумулятор — **текстуры**: движок за сессию загружает ~790 МБ
(cumulative, twodMB), живых ~60 МБ, остальное (~730 МБ) — удалённые текстуры, память
которых драйвер mali не возвращает. Оверлей был минорным вкладчиком; основа — драйвер.

### 8.3. Тест теории: возвращает ли драйвер freed GPU-ресурсы (`port/tools/gputest.c`)

Автономный тест: создаёт GLES2-контекст (SDL2 + наш мост), циклически создаёт и
удаляет ресурсы, меряя mali0 (сумма `/proc/self/maps` /dev/mali0) и VmRSS.

Результаты на устройстве:

| Сценарий | mali0 | VmRSS | Вывод |
|---|---|---|---|
| RGBA текстуры 1024² (200× create+delete, один размер) | ~4 МБ (стабильно) | ~19 МБ | переиспользует |
| RGBA текстуры, перемешанные размеры 128..2048 (400×) | ~21 МБ (стабильно) | ~49 МБ | переиспользует |
| ETC1 текстуры, перемешанные размеры (400×) | ~2.8 МБ (стабильно) | ~30 МБ | переиспользует |
| VBO (glBufferData), перемешанные размеры (400×) | ~21 МБ (стабильно) | ~49 МБ | переиспользует |
| re-spec ОДНОГО буфера 4 МБ ×500 | ~4 МБ (стабильно) | ~31 МБ | переиспользует |
| keep=1: 200 текстур, затем delete-all | 831→462 МБ VmRSS | ~450 МБ осталось | **purge ленивый** |

**Вывод: в изоляции драйвер mali ПЕРЕИСПОЛЬЗУЕТ freed-память текстур и буферов —
не накапливает.** Единственный признак удержания — ленивый purge после массового
delete-all (~450 МБ не сразу возвращается). Теория «драйвер не отдаёт ресурсы»
прямым тестом НЕ подтверждена в простых сценариях.

Значит, накопление в игре (mali0 658 МБ при живых ~76 МБ) даёт ДРУГОЙ механизм —
кандидаты: движок держит ресурсы (рендер-таргеты/FBO, кэш), либо особый путь
аллокации (device/growable memory), не воспроизводимый простыми glTexImage2D.
Дальнейшее: сравнить allocation-путь игры (по maps) с тестом; проверить рендер-таргеты.

### 8.4. Оверлей НЕ причина (проверено)

После фикса оверлея (client-side вершины, без per-frame `glBufferData`) liveBuf упал
с ~66 000 до ~5 000 за сессию — вклад оверлея убран. Тем не менее игра СНОВА
OOM-нулась (file-rss 809 МБ, mali0 658 МБ) с теми же цифрами текстур:
`twodMB=507` (cumulative), живых `liveTexBytesMB=38`. **Оверлей-курсор исключён как
причина** — накопление остаётся и без него. Виноваты текстуры движка + удержание драйвера.

### 8.5. Момент пойман: постепенное накопление, не взрыв (посимвольный лог `[glu]`)

В мост добавлен лог КАЖДОЙ загрузки текстуры с таймстемпом и нарастающим итогом
(`[glu] ts comp/tex2d WxH lvl=N +KB total=MB`). Данные на момент OOM (PID 15662):

```
glu-загрузок за сессию: 2465 (2141 comp + 324 tex2d), все с полными mip-цепочками
первая загрузка ts=3881249, последняя ts=4416955  (последняя за ~40 с до OOM)
на OOM:  liveTex=59 (gen=536, del=477)  liveTexBytesMB=68  twodMB=547  liveVBO=856 (13 МБ)
OOM: file-rss 828 МБ, mali0 ~658 МБ
```

- **Накопление ПОСТЕПЕННОЕ**: движок равномерно грузит текстуры 512²/256² (с mip-цепочками),
  наращивая итог до 547 МБ за сессию. Взрыва/пачки в одну секунду нет.
- Движок удаляет 477 из 536 текстур (del/ген сбалансированы), живых ~59 (68 МБ).
- Драйвер удерживает ~580 МБ «удалённых» текстур → mali0 658 МБ → OOM.

### 8.6. Почему изолированный тест не воспроизводит

`gputest.c` во ВСЕХ сценариях (RGBA/ETC, 512..4096, mip-цепочки, re-spec, create+delete)
показал: драйвер ПЕРЕИСПОЛЬЗУЕТ freed-память (mali0 bounded). Игра же накапливает.
Различие — **полный рендер**: игра загружает текстуру, РИСУЕТ ею, затем удаляет.
Драйвер удерживает память отрисованных текстур (пока GPU не завершит работу с ними,
а purge ленив), поэтому при непрерывном рендере накопление опережает освобождение.
В изолированном тесте (без рендера) память сразу переиспользуется.

Практичный вывод: фикса на нашей стороне без вмешательства в драйвер нет;
авто-перезапуск (`play-loop.sh`) сбрасывает накопление.

### 8.7. Эталонный тестер (`gputest.c`) — полный цикл «как игра»

Тестер умеет: текстуры RGBA/ETC1, размеры 128..4096, полные mip-цепочки, отрисовка
текстурированного квада (шейдер), re-spec буферов, режим «живого набора»
(держать до N текстур живыми, FIFO-цикл), `glFinish`, интервал замера.

**Длинные прогоны (100 000 циклов, live=60, ETC 512²+mips, create+render+delete):**

| Метрика | Старт | 100 000 циклов | Вывод |
|---|---|---|---|
| mali0 (device VA) | 0.7 МБ | 96.6 МБ | **растёт** (не отдаёт маппинги) |
| VmRSS (физическая) | 11 МБ | 41.8 МБ | **идеально ровно** — переиспользуется |

**RGBA 1024² (4 МБ), live=60, 20 000 циклов:** VmRSS стабильно 365 МБ (bounded).

**Вывод: драйвер НЕ утекает физической памятью текстур.** Растёт только виртуальная
область device-маппингов (VA-фрагментация ETC), физика переиспользуется. `glFinish()`
не снимает рост VA. Значит, физические 828 МБ file-rss у игры — НЕ от жизненного
цикла текстур: что-то в полном рендере игры делает маппинги физически закреплёнными
(кандидаты: непрерывная отрисовка со связанными текстурами, рендер-таргеты/FBO).

**Форматы текстур игры (по `[glu]`-логу):** ~87% сжатые ETC1 (`fmt=0x8d64`, 512²/256²,
mip-цепочки, ~128 КБ), ~13% обычные RGBA (`glTexImage2D`), включая **102 текстуры
1024×1024 (по 4 МБ, ~408 МБ cumulative)** — интерфейсные. RGBA 1024² — главный объём
cumulative; сжатые мелкие.

### 8.8. Watchdog: дамп smaps + базовый снимок для диффа

`host/gof2hd.c` сторожевой поток теперь при зависании пишет `/proc/self/smaps`
(`stall_<pid>.smaps`) — физические страницы по каждому маппингу, а каждые 60 с
обновляет `hang_capture/baseline.smaps` (стабильное состояние). Диф базовый ↔ в момент
OOM показывает, какие маппинги физически выросли (что и есть источник 828 МБ).

### 8.9. Источник физических 800 МБ найден: /dev/mali0 (GPU-память)

Диф smaps не сработал из-за особенностей ядра, но это само по себе — диагноз:

- smaps суммарно показывает только **~62 МБ** при VmRSS **854 МБ**. Для маппингов
  `/dev/mali0` ядро **не печатает `Rss:`** — резидентные страницы device-маппингов
  не отображаются в smaps.
- В момент OOM: **6195 маппингов /dev/mali0, 650 МБ VA** (в стабильном состоянии было
  ~540 маппингов). Анонимных больших маппингов нет.
- Следовательно, **физические ~800 МБ file-rss = GPU-память /dev/mali0** (текстуры и
  буферы), которая **физически закрепляется при непрерывной отрисовке**: драйвер не
  возвращает маппинги удалённых ресурсов (VA растёт), а игра рисует ими — страницы
  остаются резидентными.

Это объясняет, почему эталонный тестер (без непрерывного рендера) воспроизводил рост
только VA, а у игры растёт и физика. Итог: OOM вызывается GPU-памятью /dev/mali0,
которую драйвер не возвращает при длительной игре; единственный on-our-side фикс —
сокращение GPU-памяти (половинение текстур) либо авто-рестарт.

### 8.10. Эксперименты: glFinish, glBufferData→SubData, половинение RGBA

Результаты (все — НЕ устраняют OOM):

1. **`GOF_GLFINISH=60`** (периодический `glFinish` каждые 60 кадров): OOM всё равно
   (file-rss 799 МБ). mali0 прыгнул 112→657 МБ за ~6 с при замороженных GL-счётчиках
   (без новых загрузок) — физика закрепляется лавинообразно, когда GPU трогает
   накопленную VA при входе в тяжёлую сцену.
2. **`glBufferData→glBufferSubData`** (внедрено в мост, same-size re-spec): `subdata=0` —
   конверсия НЕ срабатывает, движок создаёт новые буферы (новые ID), а не re-spec тех же.
3. **Половинение RGBA 1024²→512²** (усреднение 2×2, в мосте): liveTexBytesMB упал
   60→20 МБ, но **file-rss при OOM всё равно 812 МБ**. Значит, накопление — НЕ в
   текстурах: число маппингов mali0 (~6000) совпадает с числом вызовов `glBufferData`
   (~6000) — **основной вкладчик — БУФЕРЫ (VBO)**, которые движок создаёт тысячами
   за сессию, а драйвер удерживает их device-маппинги.
   **Эксперимент ОТКАЧЕН/отменён** — половинение убрано из моста (не помогло, меняло картинку).

Итог: полный on-our-side фикс не найден; практичный — авто-рестарт (`play-loop.sh`),
полный — драйвер mali/kbase (освобождать device-маппинги удалённых буферов/текстур).

### 9. Прочие проверки

- OBB (~1 ГБ) **не mmap'ится** движком (в `/proc/PID/maps` нет `obb`); чтения идут
  через `read()` → общий page cache, а не в file-rss процесса.
- Повторные чтения одних файлов (`gof2_interface.aei` до ~1100 раз за сессию) есть и в
  стабильных, и в OOM-прогонах — **не являются причиной** (текстура кэшируется).
- `GOF_DISPLAY=WxH` (оверрайд разрешения, переданного движку) не меняет выбор текстур
  (движок всё равно грузит `gof2_interface.aei` и `textures/low/*`) — этот рычаг не работает.
- swap-файл 384 МБ НЕ спасает: OOM всё равно происходит (GPU/device-память не
  своппится). Диск на устройстве ~91% заполнен.
- zram недоступен: `CONFIG_ZRAM` не собран, модуля нет.
- cgroup v2 memory-контроллер включён (`memory` в subtree_control), но рециклить
  unevictable GPU-страницы cgroup не может.
- CMA всего 64 МБ (`CmaTotal:65536kB`) — для GPU-текстур это не основной лимит
  (маппинги mali0 до 456 МБ — это обычные страницы, не CMA).

## Почему «после фикса звука»

Вероятно, совпадение: до починки аудио игра была практически неиграбельна, кампанию
не гоняли. После фикса звука игру стали реально играть (летать по секторам), и
всплыла ресурсная проблема, которая раньше просто не достигалась.

## Инструменты (добавлены в port/tools/ и хосте)

- `port/tools/watch-hang.sh` — сэмплинг TID/RSS каждые 2 с + SIGQUIT-core по таймеру.
- `port/tools/catch-hang.sh` — детектор спина потока (~90% CPU) + замирания главного
  потока → gcore + gdb `thread apply all bt`.
- `port/tools/mem-tracker.sh` — пофайловый RSS (smaps) каждые 10 с.
- `host/gof2hd.c`:
  - сторожевой поток (watchdog): следит за VmRSS каждые 200 мс (дамп при скачке >60 МБ)
    и за renderstep (>6 с без тиков → дамп); пишет `/proc/self/maps`/`status` напрямую
    из потока (без fork) и gdb `thread apply all bt` в фоне (с очисткой LD_PRELOAD);
    после дампа принудительно завершает процесс (SIGKILL), чтобы девайс не замирал.
    Файлы: `/root/gof2hd/hang_capture/{spike|stall}_<pid>.{maps,status,bt}`.
  - `GOF_DISPLAY=WxH` — оверрайд разрешения, отдаваемого движку (для экспериментов).
- `gles-stub/gles-bridge.c` — счётчики `[gltex]`: liveTex/gen/del, tex2d/comp (~texMB),
  FBO/RB, VBO (vboGen/del), glBufferData/subdata; логируются раз в 10 с; `BIG`-логи
  для гигантских одиночных аллокаций.
- `fmodex-stub/` — не менялись (аудио не виновато).
- ПК-сэмплер `port/tools/pc_track.sh` — VmRSS/mali0/gltex каждые ~5 с + снимок maps
  при скачке.
- `port/tools/gputest.c` — эталонный тестер GLES2: текстуры RGBA/ETC1 (с mips),
  рендер квада, re-spec, живой набор (FIFO), glFinish, интервал замера. Запуск:
  `LD_LIBRARY_PATH=.:/usr/lib32 /tmp/gputest N size keep vary etc bufs respec mips render live finish interval`.

## Контрольные эксперименты (внедрены, кандидаты на проверку)

По рекомендации (см. §8.9/§8.7 — тестер опровергает простую «утечку удалённых текстур»):
ищем конкретный тип GPU-allocation, который при полном render pipeline превращает
VA-рост в физический RSS-рост.

1. **`glBufferData` → `glBufferSubData`** (в `gles-stub/gles-bridge.c`, включено):
   если `glBufferData` пере-специфицирует буфер ТОГО ЖЕ размера с данными — теперь
   вместо пересоздания GPU-хранилища вызывается `glBufferSubData` (in-place update,
   без re-alloc). Счётчик `subdata` в `[gltex]` покажет активность. Проверяем на
   игровых VBO: если mali0/VmRSS растут медленнее — старый storage держался GPU.
2. **Полный трекинг renderbuffer/FBO** (в мосте): `glRenderbufferStorage` с размерами
   (`rbBytesMB` в `[gltex]`) + gen/del FBO/RB. Проверяем, не растут ли рендер-таргеты.
3. **Периодический `glFinish()`** (в хосте, env `GOF_GLFINISH=N` — звать каждые N
   кадров). Если freed-ресурсы держатся GPU command queue — рост mali0/VmRSS замедлится.
   Запуск: `GOF_GLFINISH=60 bash start-game.sh`.

## Этап B — исследование драйвера (VBO allocation path)

Гипотеза (сильная корреляция, не финал): накопление = device-маппинги удалённых VBO.
Число маппингов `/dev/mali0` (~6000) совпадает с числом вызовов `glBufferData` (~6000),
но изолированный gputest показал, что драйвер умеет переиспользовать VBO в простом
create/delete. Значит, надо доказать/опровергнуть удержание в игровом контексте.

**Трекер жизненного цикла VBO (внедрён в мост):**
- кольцевой буфер 8192 записей: `gen, id, size, data_ts, del_ts, live`;
- `glBufferData` → запись/обновление (data_ts); `glDeleteBuffers` → del_ts, live=0;
- дамп при зависании: `hang_capture/vbo_tracker.txt` (заголовок: created/live/deleted;
  все записи). Вызывается watchdog'ом из хоста (`gof_vbo_tracker_dump`).

**Как читать результат (корреляция с mali0):**
1. В `vbo_tracker.txt`: `created` — сколько VBO выделено за сессию, `live` — сколько
   сейчас живы, `deleted` — сколько удалено.
2. В `stall_*.maps`: число маппингов `/dev/mali0`.
3. Если `mali0_mappings ≈ created` (а не `live`) → удалённые allocations остаются в
   драйвере (подтверждение). Если `mali0_mappings ≈ live` → драйвер освобождает, ищем
   иначе.
4. Дополнительно: тип kbase-памяти VBO через debugfs `mali0/ctx/<pid>_0/mem_view`
   (growable/fixed) — почему не под reclaim.

**Если подтвердилось** → путь в ядро: `glDeleteBuffers → libmali → ioctl →
kbase_mem_free/unmap`, почему страницы не возвращаются (shrinker/unevictable).
Запуск диагностической сессии: `bash start-game.sh`, играем до зависания; при OOM
watchdog пишет `stall_<pid>.{maps,smaps,status}` + `vbo_tracker.txt`.

### Результат Этапа B (ПОДТВЕРЖДЕНО)

Сессия 30023, при OOM (file-rss 812 МБ):

| Метрика | Значение |
|---|---|
| mali0 маппингов в момент OOM | **6633** (535 МБ) |
| VBO created (трекер `vbo_tracker.txt`) | **7013** |
| VBO live | 2723 |
| VBO deleted | 4290 |
| mali0 маппингов в стабильном состоянии | 547 |

**mali0_mappings (6633) ≈ created (7013), а не live (2723).** Гипотеза подтверждена:
движок создаёт ~7000 VBO за сессию (`glBufferData`), удаляет ~4300 (`glDeleteBuffers`),
но драйвер mali/kbase **удерживает device-маппинги всех созданных** (включая удалённые) —
mapping_count растёт с `created`, а не с `live`. Физика закрепляется при непрерывной
отрисовке → OOM. Распределение размеров маппингов (4472 <64К, 1940 64-256К) —
типичные мелкие VBO.

Дальше: путь в ядро — `glDeleteBuffers → libmali → ioctl → kbase_mem_free`, почему
маппинги не возвращаются (тип kbase-аллокации VBO, shrinker/unevictable). Практичный
обход — авто-рестарт (`play-loop.sh`).

### Контрольный baseline (для оценки любых фиксов)

Сессия 30023 — эталон для проверки, что драйвер починен:

```
created VBO      = 7013
deleted VBO      = 4300
live VBO         = 2723
mali0 mappings   = 6633
~800 МБ file-rss → OOM
```

**Критерий фикса:** после изменения kbase/libmali mappings должны следовать за
`live VBO`, а не за `created VBO`. Если `mali_mappings ≈ live` — проблема решена.

### Эксперимент C1: dummy-текстура (ПРОРЫВ — текстуры триггер)

В мост добавлен режим `GOF_DUMMY_TEX=1`: `glBindTexture` подставляет 1×1 серую
текстуру вместо реальной, а `glTexImage2D`/`glCompressedTexImage2D` НЕ загружают
данные (только счётчики). Геометрия/VBO и полный рендер-цикл остаются.

**Результат (сессия 33618, играл до 16+ минут, без падения):**

| Метрика | С реальными текстурами | С dummy (GOF_DUMMY_TEX) |
|---|---|---|
| mali0 | растёт до 658 МБ | **25 МБ, стабильно** |
| mali0 mappings | 6633 | **216** |
| VmRSS | растёт до 854 МБ | **126 МБ, стабильно** |
| VBO создано (gen) | ~7000 | **~19826** |
| Итог | OOM на ~10-17 мин | **не падает, 16+ мин** |

**Вывод: триггер накопления — ЗАГРУЗКИ ТЕКСТУР, а не VBO.** При ~20 000 VBO и без
реальных текстур mali0 = 25 МБ. Каждая реальная `glTexImage2D/glCompressedTexImage2D`
создаёт GPU-маппинг, который драйвер не возвращает при непрерывном рендере
(движок загрузил twodMB=1678 МБ cumulative, мост их отбросил — и памяти не выросло).

Корреляция «mali0 ≈ created VBO» была ложной: счётчики загрузок текстур и буферов у
игры совпадали по стечению обстоятельств. Половинение текстур не помогло, потому что
накопление — **на каждую загрузку** (новый маппинг), а не на размер.

**Следующее:** Этап C2 — трассировка ioctl именно `glTexImage2D/glDeleteTextures`
(а не VBO), и поиск, почему маппинги удалённых текстур не возвращаются.

### Эксперимент C1.5: апскейл текстур ×2 (обратный, «по приколу»)

В мост добавлен режим `GOF_UPSCALE=1`: RGBA 2D level-0 текстуры увеличиваются ×2
(nearest-neighbour), память на текстуру ×4.

| Метрика | Обычные | Апскейл ×2 |
|---|---|---|
| Время до OOM | ~10-17 мин | **~8 мин** |
| total-vm при OOM | ~800 МБ | **~526 МБ** |
| file-rss при OOM | ~810 МБ | **~811 МБ** (тот же порог) |
| liveTexBytesMB | ~55 МБ | **~296 МБ** (×4) |

**Вывод:** размер текстур влияет на скорость заполнения физики (живой набор ×4 →
VmRSS 500 МБ), но порог OOM (file-rss ~810 МБ) одинаков. Вместе с C1 (dummy —
стабильно) подтверждено: **загрузки текстур = триггер накопления; размер текстур =
скорость заполнения.** Эксперимент убран из моста (режим за гейтом `GOF_UPSCALE`).

### Эксперимент C1.6: пул текстур и буферов (переиспользование ID)

`GOF_TEX_POOL=1`: `glDeleteTextures`/`glDeleteBuffers` НЕ уничтожают физические
ресурсы (ID в free pool), `glGen*` возвращают ID из пула первыми. Цель — ограничить
число маппингов пулом вместо общего числа загрузок.

**Результат:** пул переиспользовал 179-406 текстур и до 3264 буферов, но:
- игра всё равно дошла до **881 МБ VmRSS (file-rss 799 МБ)** — того же потолка;
- mali0 маппингов стало **13151** (БОЛЬШЕ, чем без пула 6633) — пул держит ресурсы
  живыми, раздувая маппинги;
- итог — render loop stall (картинка встала, звук ещё шёл), watchdog убил процесс.

**Пул НЕ работает** (усугубляет — держит слишком много живого). Откачен из моста.
Финальный вывод: игра упирается в потолок ~810 МБ file-rss независимо от
on-our-side GL-манипуляций (downscale, glFinish, SubData, pools). Практичный путь —
авто-рестарт `play-loop.sh`; полный фикс — только в драйвере mali/kbase.

### Эксперимент C1.7: texture lifecycle reproducer (`port/tools/textest.c`)

Автономный тест: `for(;;){ glGenTextures; glBindTexture; glTexImage2D; glTexParameteri;
glDeleteTextures; }` (RGBA 512², варианты A-E с glFinish/SwapBuffers), замер
mali0-mappings / VmRSS / RssFile.

**Результат (вариант A, 100 000 загрузок, 512²):**

```
start  mali0 maps=7  (28 KB)   VmRSS=11136  RssFile=8964
iter 1      mali0 maps=8 (1052 KB)  VmRSS=13264  RssFile=9996
iter 10001  mali0 maps=8 (1052 KB)  VmRSS=13264  RssFile=9996
... (все 100k — константа)
```

**Чистый texture churn НЕ воспроизводит накопление** — mali0 maps=8, VmRSS/RssFile
стабильны. Драйвер переиспользует идеально. Значит, накопление в игре возникает
ТОЛЬКО во взаимодействии с полным рендером (непрерывная отрисовка + перезагрузки +
GPU-очередь), и в изоляции не воспроизводится никакими тестами (VBO, текстуры, пулы,
glFinish).

**Следующий шаг (план):** strace реальной игры на `/dev/mali0` — асимметрия
mmap/munmap/ioctl (mmap≈uploads vs munmap≈deletes). Если mmap много, munmap мало —
userspace retention в libmali; если симметрично — kernel-side kbase bookkeeping.
Запуск: `strace -f -e trace=mmap,munmap,ioctl,close -o /tmp/mali.log ./gof2hd ...`,
затем отфильтровать ioctl именно fd `/dev/mali0`.

Примечание: посимвольный лог загрузок текстур (`[glu] ...`) теперь за гейтом
`GOF_GLU_LOG` (по умолчанию выкл), чтобы не заливать лог при массовых загрузках.

## Этап C — трассировка `glDeleteBuffers → libmali → ioctl → kbase`

Главный вопрос: **доходит ли `glDeleteBuffers()` до kernel-side освобождения
allocation?** Возможны три варианта:

1. libmali вообще НЕ вызывает нужный ioctl при удалении буфера;
2. ioctl вызывается, но kbase откладывает освобождение;
3. kbase освобождает объект, но оставляет mapping/physical pages из-за типа allocation.

Зависимость `mali_mappings ∝ created` (а не `live`) подтверждает именно
create→delete→create→delete-цикл как источник накопления маппингов.

**Шаги этапа C:**
1. `ls -l /dev/mali*` — какие устройства; определить ioctl'ы, используемые `libmali.so`
   (по таблице ioctl-диспетчера драйвера, коды).
2. В kbase-исходниках (если есть) искать путь: `kbase_mem_alloc`, `kbase_mem_free`,
   `kbase_mem_pool`, `kbase_gpu_mmap`, `kbase_cpu_mmap`, `kbase_mem_uncommit` и
   ioctl-dispatch handlers.
3. Особый интерес: **тип kbase-аллокации, которую libmali создаёт для VBO** (что просит
   у kbase: growable/fixed/cpu vs gpu). Не трогать shrinker вслепую — сначала понять,
   что именно libmali запрашивает.
4. Трассировать ioctl на устройстве: `strace -e ioctl` (если есть) на процесс игры,
   либо через `LD_PRELOAD`-обёртку `ioctl` в мосте — логировать `glDeleteBuffers` →
   какие ioctl с какими кодами идут в `/dev/mali*`.

**Практично (параллельно):** играть через `play-loop.sh` — рабочий fallback, устройство
не превращается в кирпич.

## Пути решения

Проблема: драйвер mali (kbase/libmali) не возвращает системе GPU-страницы удалённых
текстур/буферов за сессию → накопление ~580-730 МБ за 10-40 мин → OOM на девайсе с
973 МБ. Ниже пути решения от быстрых к глубоким.

### 1. Авто-перезапуск (работает, рекомендую) — `port/tools/play-loop.sh`
- Запускает игру в цикле; после OOM/зависания (watchdog сам завершает процесс) сразу
  перезапускает. Свежий процесс = нулевое накопление = как после перезагрузки.
- **Плюсы:** мгновенно, без риска, решает «девайс замирает намертво».
- **Минусы:** не устраняет причину; на «тяжёлых» планетах игра всё равно падает
  (но продолжает работать после рестарта).
- Запуск: `bash /root/gof2hd/port/tools/play-loop.sh`.

### 2. Конвертация `glBufferData` → `glBufferSubData` в мосте (средне, не испытано)
- Движок и оверлей вызывали `glBufferData` (пересоздание буфера) каждый кадр для
  динамической геометрии. Каждая re-spec — новая GPU-аллокация, которую драйвер
  может удерживать.
- Идея: в `gles-stub/gles-bridge.c` если буфер уже имеет размер ≥ нового (отслеживается
  трекером `g_vbuf`) и данные не NULL — вызывать `glBufferSubData(0, size, data)`
  вместо `glBufferData`, не пересоздавая GPU-хранилище.
- **Оверлей уже исправлен** (рисует client-side, см. §8.2). Осталось для движка.
- **Плюсы:** убирает per-frame пересоздания буферов; безопасно при одинаковом размере.
- **Минусы:** текстуры — основной вкладчик (~730 МБ), буферы — меньший (~130 МБ);
  эффект ограниченный. Семантическая правка (нужен регресс-тест рендера).

### 3. Уменьшение размера текстур в мосте (большой эффект, сложно)
- Текстуры — главный аккумулятор (790 МБ cumulative, 60 МБ живых). Драйвер держит
  удалённые. Если уменьшить размер каждой загрузки в ~4 раза (половинение), накопление
  упадёт в ~4 раза и влезет в 973 МБ.
- Идея: в `glTexImage2D`/`glCompressedTexImage2D` декодировать и половинить
  (ETC1: декод→половинение→ре-энкод; RGBA: усреднение 2x2), загружать меньшую версию.
- **Плюсы:** атакует именно основной вкладчик; игра на 640×480 почти не теряет в
  качестве.
- **Минусы:** ~150-300 строк кода (ETC1 кодек), CPU-нагрузка при загрузке, риск
  артефактов. Кандидат для следующего этапа.

### 4. Кэш повторных загрузок текстур в мосте (средне, сложно)
- Движок перезагружает одни и те же текстуры при повторном входе в зону (интерфейс
  читается до ~1100 раз/сессию). Если бы мост отдавал ранее загруженный GPU-объект,
  cumulative загрузок упал бы.
- **Минусы:** надо уметь «переиспользовать» GL-объекты движка без нарушения его
  bookkeeping; риск.

### 5. Драйвер mali/kbase (полный фикс, глубокая kernel-работа)
- Выяснить, почему freed-страницы не возвращаются: `pages_scanned:0` при OOM, пулы
  kbase пусты, куб-текстур нет. Кандидаты: libmali не сообщает kbase о free, либо
  kbase не отдаёт страницы в allocator на этом ядре 4.9 Allwinner.
- Возможные шаги: проверить более свежую `libmali.so` с других прошивок/устройств
  Allwinner; поискать shrinker/параметры kbase; собрать новый модуль `mali_kbase`
  (если есть исходники ядра).
- **Плюсы:** единственный путь к полному устранению OOM без компромиссов.
- **Минусы:** очень трудозатратно, без гарантий.

## Статус / открытые вопросы

- [x] Поймать момент зависания и определить механизм → OOM, GPU/device-память
      (definitive дамп: RssFile 819-828 МБ, mali0 658 МБ).
- [x] Стабильный триггер: перелёт между планетами / длительная сессия.
- [x] Проверить swap/drop_caches/разрешение/качество/куб-текстуры/glFinish/половинение → не помогают.
- [x] Оверлей-курсор исключён (исправлен; накопление осталось).
- [x] Форматы текстур: ETC1 (~87%) + RGBA 1024² (~408 МБ cumulative).
- [x] Эталонный тестер (100 000 циклов): в изоляции драйвер переиспользует (VA растёт, физика — нет).
- [x] Источник физических ~800 МБ = маппинги `/dev/mali0` (§8.9), физически закреплённые рендером.
- [x] **Этап B: подтверждено** — mali_mappings ∝ created VBO (6633 ≈ 7013), не live (2723).
      Драйвер удерживает device-маппинги удалённых VBO.
- [x] C1 (dummy-текстуры): без загрузок текстур игра стабильна 17+ мин при ~20 000 VBO.
- [x] C1.5 (апскейл ×2): размер текстур влияет на скорость заполнения, порог тот же.
- [x] C1.6 (пулы текстур+буферов): НЕ работают, усугубляют (13151 маппингов).
- [x] C1.7 (texture churn reproducer, 100k): чистый churn текстур ПОЛНОСТЬЮ bounded
      (mali0 maps=8) — накопление только во взаимодействии с полным рендером.
- [ ] **Этап C (следующий):** strace игры на `/dev/mali0` — асимметрия mmap/munmap/ioctl
      (userspace libmali vs kernel kbase retention).
- [ ] **Практично:** `play-loop.sh` — основной launcher (fallback), авто-рестарт
      сбрасывает накопление.

## Финальный вывод (2026-08-16)

**Проблема:** игра (Android-движок GOF2) через 10-17 мин активной игры (перелёты между
планетами) упирается в потолок ~810 МБ file-rss на девайсе с 973 МБ → OOM/зависание.

**Механизм (подтверждён всей цепочкой экспериментов):**
- Накопление — в GPU-памяти драйвера mali (`/dev/mali0` маппинги, ~6000-13000 шт.,
  ~650 МБ), физически закрепляемой непрерывным рендером.
- Триггер — **загрузки текстур** (движок перезагружает одни и те же текстуры сотни
  раз; cumulative до 1678 МБ при ~50 живых). C1 (dummy, без загрузок) — игра стабильна
  даже при 20 000 VBO.
- Ни один on-our-side GL-фикс не решает: половинение, апскейл, glFinish,
  glBufferData→SubData, пулы ресурсов, разрешение, swap, drop_caches.
- Изолированные тесты (textest 100k churn, vbotest 20k, gputest) — все bounded:
  драйвер переиспользует в изоляции. Накопление возникает ТОЛЬКО во взаимодействии
  загрузок текстур с полным рендер-циклом игры (GPU-очередь не даёт драйверу
  рециклировать маппинги удалённых ресурсов).

**Почему PC-порты на этой консоли работают:** PC-движки грузят текстуры один раз и
держат стабильный пул; Android-движок GOF2 пересоздаёт их тысячами за сессию — паттерн,
который этот mali (r20p0) не переживает при непрерывном рендере.

**Практичный путь:** играть через `play-loop.sh` (авто-рестарт после зависания/OOM —
накопление сбрасывается, как перезагрузка). Полный фикс — только в драйвере
mali/kbase (освобождать device-маппинги удалённых текстур/буферов при длительном
churn). Следующий диагностический шаг — strace игры на `/dev/mali0` (mmap/munmap/ioctl
асимметрия), чтобы определить, где retention: userspace libmali или kernel kbase.

## Команды для повторной диагностики (кратко)

```sh
# на устройстве: включить диагностику и запустить игру
export OPENSL_DEBUG=1; bash /root/gof2hd/port/tools/start-game.sh

# на ПК: сэмплер VmRSS/mali0/gltex + захват maps при скачке
setsid -f bash /tmp/opencode/pc_track.sh >/tmp/opencode/pc_track_out.log 2>&1 </dev/null

# при зависании дампы сами появятся в /root/gof2hd/hang_capture/ (watchdog)
# разбор: gdb-multiarch -batch -ex 'set solib-search-path <run-native>' \
#   -ex 'thread apply all bt' ./gof2hd /root/gof2hd/hang_capture/watchdog_core

# dmesg после OOM:
dmesg | grep -B2 -A12 "Out of memory"
```
